<%= link_to gravatar_for(current_user, size: Settings.user_gravatar),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("view_my_pro"), current_user %></span>
<span><%=  t("micro"), count: current_user.microposts.size %></span>
